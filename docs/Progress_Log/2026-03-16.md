# 2026-03-16 (월): Boss Attack1 공격범위 source 정리 & NGO/UTP/Relay/Lobby 패키지 및 서비스 초기화 & attack1 준비동작 구현

# Boss Attack1 공격범위 source 정리
## 오늘 반영한 작업
* `DamageCaster`에 현재 반경을 읽는 `Radius` accessor를 추가해 Boss Attack1 범위의 단일 참조값으로 사용할 수 있게 했다.
* `BossController.basicAttackRange`는 Inspector에서 숨기고, `BossController.BasicAttackRange`가 `HeadDamageCaster.radius`를 직접 읽도록 바꿨다.
* 기존 `BossController -> HeadDamageCaster.SetRadius(...)` 동기화 경로를 제거하고, Basic range gizmo도 같은 `HeadDamageCaster.radius` 기준으로 그리도록 정리했다.
* `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 오늘 구현 기준으로 동기화했다.
* `Milestone_Backlog`, `Boss_Algorithm_Design`의 Attack1 공격범위 설명도 현재 구현 기준으로 정리했다.

## 체크리스트 업데이트
* [x] `BossController`의 `basicAttackRange`를 Inspector에서 보이지 않게 정리하기
* [x] Attack1 조정 기준을 `BossController.basicAttackRange`가 아니라 `DamageCaster.radius`로 통일하기

## 맥락노트
* 현재 백로그에는 Attack1 공격범위를 `DamageCaster.radius` 대신 `BossController.basicAttackRange`로 수정하는 흐름이 적혀 있다.
* 오늘 작업 목표는 `basicAttackRange`를 Inspector 조정 포인트에서 내리고, 실제로 수정 가능한 공격범위 기준을 `DamageCaster.radius` 하나로 정리하는 것이다.
* 기존 씬 데이터(`GamePlayScene`, `GamePlayScene Scene_TestResult1`)를 확인했을 때 `basicAttackRange`와 `HeadDamageCaster._radius` 값이 이미 동일하게 저장돼 있어 source 전환 리스크는 낮았다.

## 구현 계획
### 목표
* Attack1의 사거리 판정과 실제 타격 반경이 모두 `HeadDamageCaster.radius`를 기준으로 동작하도록 바꾼다.
* `BossController.basicAttackRange`는 Inspector 조정 포인트에서 제거하고, Attack1 범위는 `DamageCaster` 쪽에서만 수정하게 만든다.

### 영향 파일
* `Assets/Scripts/Common/Combat/DamageCaster.cs`
* `Assets/Scripts/Boss/BossController.cs`
* `Assets/Scripts/Boss/BossFSM.cs` (필요 시 범위 참조 경로 정리)

### 변경 순서
* `DamageCaster`에 현재 반경을 읽을 수 있는 public read accessor를 추가한다.
* `BossController.BasicAttackRange`가 직렬화 필드가 아니라 `HeadDamageCaster.radius` 값을 반환하도록 바꾼다.
* `BossController`의 `basicAttackRange` 필드는 Inspector에서 숨기고, 기존 `SyncBasicAttackRangeToHeadDamageCaster()` 기반 동기화 경로는 제거한다. hidden field는 `HeadDamageCaster` 미할당 시 fallback 직렬화 값으로만 남긴다.
* Phase1의 Basic 패턴 선택, chase hysteresis의 최대 사거리 계산, gizmo 반경 표시가 모두 같은 `HeadDamageCaster.radius` 값을 읽도록 정리한다.
* `BasicAttackPattern`은 이미 `HeadDamageCaster.EnableHitbox(...)`를 사용하고 있으므로, 공격 실행 경로는 유지하고 range source만 단일화한다.

### 설계 근거
* `System_Blueprint`의 `Boss Basic Range-Hitbox Sync Rule`은 사거리 판정과 실제 타격 반경이 어긋나지 않도록 같은 값을 유지해야 한다고 정의하고 있다.
* 이번 변경은 sync direction을 `BossController -> DamageCaster`에서 `DamageCaster = single source of truth`로 바꾸는 작업이다.
* Inspector에서 같은 의미의 값을 두 군데 수정할 수 있으면 유지보수 중 혼선이 생기므로, 조정 포인트를 하나로 줄이는 편이 안전하다.

### 리스크
* 기존 프리팹/씬에서 `basicAttackRange`와 `HeadDamageCaster.radius`가 서로 다른 값이었다면, source of truth 변경 후 체감 사거리가 달라질 수 있다.
* `HeadDamageCaster` 참조가 누락된 프리팹에서는 Basic 사거리 계산이 비정상값이 될 수 있으므로 null fallback 또는 warning 처리 방안을 함께 검토해야 한다.
* gizmo와 실전 판정이 같은 값을 보도록 바꾸는 과정에서, 디버그 시각화가 임시로 어긋나지 않게 한 번에 정리해야 한다.

### 검증 포인트
* `BossController` Inspector에서 `basicAttackRange` 필드가 보이지 않아야 한다.
* `HeadDamageCaster`의 `radius`를 변경하면 Basic 공격 선택 거리와 실제 판정 반경이 함께 변해야 한다.
* Basic range gizmo가 `HeadDamageCaster.radius`와 같은 반경으로 그려져야 한다.
* Lunge/Projectile/AoE 패턴의 사거리 동작에는 영향이 없어야 한다.
* 코드 변경 후 `dotnet build Assembly-CSharp.csproj`로 컴파일 검사를 수행한다.

### 문서 동기화 계획
* 기준 로그는 `docs/Progress_Log/2026-03-16.md`를 사용한다.
* 코드 변경이 끝나면 `docs/technical/System_Blueprint.md`의 Basic range 규칙 문구를 `DamageCaster.radius` 기준으로 갱신한다.
* 코드 변경이 끝나면 `docs/technical/Technical_Glossary.md`에도 Attack1 range source 변경 내용을 반영한다.
* 관련 백로그/설계 문서(`docs/roadmap/Milestone_Backlog.md`, `docs/technical/Boss_Algorithm_Design.md`)의 Attack1 공격범위 설명도 같은 기준으로 맞춘다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v:minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* 현재 Basic 공격 사거리 판정은 `BossFSM`이 `Controller.BasicAttackRange`를 읽고, `BossController`가 그 값을 `HeadDamageCaster.SetRadius(...)`로 밀어 넣는 구조였다.
* 즉 기존 구현에서는 `DamageCaster.radius`가 실제 source가 아니라 `BossController.basicAttackRange`의 동기화 대상이었다.
* 기존 씬 데이터에서 `basicAttackRange`와 `HeadDamageCaster._radius`가 이미 같은 값으로 저장돼 있어, single-source 전환 시 immediate data migration은 필요하지 않았다.

### 무엇을 수정했는가
* `DamageCaster`에 `Radius` read accessor와 반경 clamp 보정을 추가했다.
* `BossController.basicAttackRange`를 Inspector에서 숨기고, `BasicAttackRange` property와 Basic gizmo가 `HeadDamageCaster.radius`를 직접 읽도록 바꿨다.
* `SyncBasicAttackRangeToHeadDamageCaster()` 호출과 메서드를 제거해 `BossController -> DamageCaster` 동기화 흐름을 없앴다.
* 오늘 로그와 `System_Blueprint`, `Technical_Glossary`, `Milestone_Backlog`, `Boss_Algorithm_Design`을 실제 구현 결과 기준으로 동기화했다.

### 왜 그렇게 판단했는가
* Attack1 범위를 `BossController`와 `DamageCaster` 두 곳에서 동시에 조절할 수 있으면 어떤 값이 실제 기준인지 계속 헷갈리므로 source를 하나로 줄이는 편이 유지보수에 안전하다.
* Boss의 패턴 선택 거리, 실제 피격 반경, gizmo가 모두 같은 값을 읽어야 튜닝 결과와 플레이 체감이 어긋나지 않는다.
* hidden legacy fallback을 남겨 두면 `HeadDamageCaster` 참조가 누락된 예외 상황에서도 기본 사거리 계산이 완전히 깨지지 않는다.

---
# NGO/UTP/Relay/Lobby 패키지 및 서비스 초기화
## 오늘 반영한 작업
* Unity Dashboard Project Link 이후 `ProjectSettings/ProjectSettings.asset`에서 `cloudProjectId`가 채워진 것을 확인했다.
* `Packages/manifest.json`에 `com.unity.services.multiplayer@2.1.3`, `com.unity.netcode.gameobjects@1.15.1`를 direct baseline dependency로 추가했다.
* Unity batchmode resolve를 실행해 `Packages/packages-lock.json`을 재생성했고, resolver가 `com.unity.transport@2.6.0`, `com.unity.services.authentication@3.6.0`, `com.unity.services.core@1.16.0` 등을 transitive dependency로 고정한 것을 확인했다.
* `Logs/unity_package_resolve.log`와 `Library/ScriptAssemblies`를 확인해 Session 1 범위의 package resolve가 끝났고, `error CS`, `Compilation failed`, `Scripts have compiler errors` 패턴이 없는 것을 확인했다.
* `Assets/Scripts/Multiplayer/Bootstrap/MultiplayerServicesBootstrap.cs`를 추가해 `UnityServices.InitializeAsync()` -> `AuthenticationService.Instance.SignInAnonymouslyAsync()` -> `PlayerId` 확보를 one-time init으로 묶었다.
* `Assets/Scripts/Multiplayer/UI/MultiplayerTitleSceneRuntimeInstaller.cs`와 `Assets/Scripts/Multiplayer/UI/MultiplayerTitleSceneDriver.cs`를 추가해 `Assets/Scenes/mutiplayer/TitleScene.unity`에서만 bootstrap 경로가 붙도록 분리했다.
* duplicated multiplayer title scene에서는 `Create Room` / `Join` 클릭 전에 bootstrap을 먼저 보장하고, 진행 중에는 버튼 라벨을 `Connecting...`로 바꾸고 전체 버튼을 잠가 중복 클릭을 막게 했다.
* bootstrap 실패 시에는 기존 wrong-key popup UI를 재사용해 오류 문구를 보여주도록 연결했다.
* duplicated multiplayer title scene의 `Solo Play` / Host `Start`는 common `SceneLoader` 대신 `Assets/Scenes/mutiplayer/GamePlayScene.unity` 경로로 직접 이동하도록 우회해 main scene으로 새 멀티 코드가 새지 않게 했다.
* `ProjectSettings/EditorBuildSettings.asset`에 duplicated multiplayer scene 3종(`TitleScene`, `LoadingScene`, `GamePlayScene`)을 추가해 path load 준비를 맞췄다.
* `dotnet build Assembly-CSharp.csproj -v:minimal`로 코드 컴파일 검사를 수행했고 Warning 3 / Error 0 결과를 확인했다.
* duplicated multiplayer title scene manual smoke check에서 `Create Room` 직전 `Connecting...` 표시와 Console `MultiplayerServicesBootstrap: Ready. PlayerId=...` 로그를 확인해 Session 2 runtime 검증까지 끝냈다.
* `Progress_Log`, `System_Blueprint`, `Technical_Glossary`, `Multiplayer_Initialization_Session_Spec`를 현재 Session 1+2 코드 상태 기준으로 동기화했다.

## 체크리스트 업데이트
* [x] Unity Dashboard에서 Lobby/Relay 활성화 후 Editor Project Link 연결 (`cloudProjectId` 확인)
* [x] `com.unity.services.multiplayer` + `com.unity.netcode.gameobjects` baseline 추가
* [x] `MultiplayerServicesBootstrap` 코드 추가
* [x] duplicated multiplayer title scene 전용 bootstrap driver 추가
* [x] Play Mode에서 `IsReady == true` + `PlayerId` 실확인
* [ ] `MultiplayerRuntimeRoot` 추가
* [ ] `MultiplayerSessionService`로 Host/Client create/join/shutdown 경로 추가
* [ ] fake host/client/lobby 로직을 실제 session service 호출로 교체
* 참고: 위 3개 후속 항목은 같은 날 아래 `Host create 세션` 섹션에서 이어서 완료했다.


## 맥락노트
* 이번 작업으로 `Session 2 - UGS bootstrap 세션`은 code path뿐 아니라 실제 Play Mode smoke check까지 확인 완료 상태가 됐다.
* duplicated multiplayer title scene도 원래는 common `TitleSceneController`와 common `SceneLoader`를 그대로 따라가 main scene으로 되돌아갈 수 있었기 때문에, 새 bootstrap 코드는 `Assets/Scenes/mutiplayer/...` path에서만 켜지도록 따로 감쌌다.
* `TitleSceneController`의 Host/Client 경로는 bootstrap 성공 이후에도 아직 fake local lobby prototype을 유지한다. 즉 지금 단계는 `UGS ready gate`를 붙인 것이지, real Lobby/Relay create/join까지 끝낸 단계는 아니다.
* manifest에는 direct dependency만 추가하고, `com.unity.transport` / `com.unity.services.authentication`은 resolver가 실제로 요구할 때만 lock에 잡히게 두는 전략을 유지했다.
* `Assets/Scripts/Multiplayer` 계층은 이번에 처음 생겼고, main title scene asset은 직접 건드리지 않은 채 duplicated multiplayer scene 전용 runtime installer로만 연결했다.


## 구현 계획
### 목표
* `Session 1` 기준선 위에 `Session 2` bootstrap 코드를 올려 `UnityServices` + anonymous sign-in one-time init을 실제 버튼 흐름 앞에 둔다.
* main title scene 안정성을 지키기 위해, 멀티 관련 새 동작은 `Assets/Scenes/mutiplayer/...` duplicated scene path에서만 켜지게 분리한다.
* Host create / Client join의 fake lobby prototype은 유지하되, 이후 Session 3/4가 바로 붙을 수 있도록 bootstrap owner와 scene path rule을 먼저 고정한다.


### 영향 파일
* `Packages/manifest.json`
* `Packages/packages-lock.json`
* `ProjectSettings/ProjectSettings.asset` (Project Link 확인)
* `ProjectSettings/EditorBuildSettings.asset`
* `Assets/Scripts/Multiplayer/Bootstrap/MultiplayerServicesBootstrap.cs`
* `Assets/Scripts/Multiplayer/SceneFlow/MultiplayerScenePaths.cs`
* `Assets/Scripts/Multiplayer/UI/MultiplayerTitleSceneRuntimeInstaller.cs`
* `Assets/Scripts/Multiplayer/UI/MultiplayerTitleSceneDriver.cs`
* `docs/technical/System_Blueprint.md`
* `docs/technical/Technical_Glossary.md`
* `docs/technical/multiplayer/Multiplayer_Initialization_Session_Spec.md`

### 변경 순서
* Unity Dashboard Project Link가 끝난 상태를 먼저 확인한다.
* `com.unity.services.multiplayer`와 `com.unity.netcode.gameobjects`를 manifest에 추가하고, direct dependency는 최소 집합으로 유지한다.
* Unity batchmode resolve를 실행해 lock file을 갱신한다.
* resolve 결과에서 `com.unity.transport`, `com.unity.services.authentication`, `com.unity.services.core` 등의 transitive dependency가 실제로 어떻게 잡혔는지 확인한다.
* `MultiplayerServicesBootstrap`에 one-time init state와 `EnsureInitializedAsync()`를 추가한다.
* duplicated multiplayer title scene에서만 동작하는 runtime installer/driver를 만들어 `Create Room` / `Join` 전에 bootstrap을 보장한다.
* duplicated multiplayer title scene의 `Solo Play` / `Start`를 duplicated gameplay scene path로 우회해 main scene 의존을 끊는다.
* `dotnet build`로 compile gate를 다시 확인하고, 문서를 현재 Session 2 코드 상태 기준으로 갱신한다.


### 설계 근거
* `Multiplayer_Initialization_Session_Spec`는 `Session 2` owner를 `MultiplayerServicesBootstrap`으로 고정하고, `InitializeAsync()` -> `SignInAnonymouslyAsync()` -> `PlayerId` 확보 순서를 요구한다.
* user 요청대로 멀티 기능을 main scene에서 떼어 두려면, main `TitleSceneController`를 새 로직으로 직접 갈아엎기보다 duplicated multiplayer title scene에서만 runtime driver를 붙이는 편이 안정적이다.
* `TitleSceneController` 내부 fake UI flow는 아직 다음 세션의 교체 대상이므로, 이번 단계에서는 reflection wrapper로 진입점만 감싸고 기존 panel/layout 구조는 그대로 재사용하는 편이 변경 범위를 줄인다.


### 리스크
* duplicated multiplayer title scene driver는 private `TitleSceneController` 멤버 이름에 의존하므로, 이후 common controller 내부 이름이 바뀌면 wrapper도 같이 갱신해야 한다.
* 현재는 bootstrap 이후에도 fake lobby logic을 호출하므로, room title/join code/connected count는 아직 real Lobby/Relay 값이 아니다.


### 검증 포인트
* `Packages/packages-lock.json`에 `com.unity.services.multiplayer`, `com.unity.netcode.gameobjects`, `com.unity.transport`, `com.unity.services.authentication`이 resolve된 상태여야 한다.
* `Assets/Scripts/Multiplayer/Bootstrap/MultiplayerServicesBootstrap.cs`가 `IsReady`, `PlayerId`, `LastErrorMessage`, one-time init guard를 제공해야 한다.
* duplicated multiplayer title scene에서 `Create Room` / `Join` 클릭 시 bootstrap 시작 전후에 `Connecting...` 상태와 버튼 잠금이 보여야 한다.
* bootstrap 실패 시 wrong-key popup 자리에 service error 문구가 떠야 한다.
* `dotnet build Assembly-CSharp.csproj -v:minimal` 결과가 Warning only / Error 0 이어야 한다.
* duplicated multiplayer title scene manual Host smoke check에서 `PlayerId` log가 실제로 찍혀야 한다.


### 문서 동기화 계획
* `docs/technical/System_Blueprint.md`의 `Title Multiplayer UI`, `Game Loop`, `Netcode Prep` 상태를 duplicated scene bootstrap 기준으로 갱신한다.
* `docs/technical/Technical_Glossary.md`에 `Services Bootstrap Session`, `Multiplayer Scene Isolation`, `MultiplayerTitleSceneDriver` 용어를 추가한다.
* `docs/technical/multiplayer/Multiplayer_Initialization_Session_Spec.md`에 Session 2 current state와 duplicated scene 적용 범위를 반영한다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v:minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: Unity batchmode smoke compile은 `Logs/unity_session2_bootstrap.log`에서 `HandleProjectAlreadyOpenInAnotherInstance`가 기록돼 project lock 때문에 중단됐다. 대신 duplicated multiplayer title scene manual smoke check에서 `Connecting...` UI와 real `PlayerId` Console log를 확인했다.


## 기술적 고려
### 무엇을 발견했는가
* direct dependency는 여전히 두 개만으로 충분했고, Session 2 구현은 그 위에 service bootstrap 코드만 추가하면 됐다.
* duplicated multiplayer title scene도 기본 상태에서는 common `TitleSceneController`와 common `SceneLoader`를 그대로 써서, 새 멀티 코드를 그대로 넣으면 main scene 흐름과 쉽게 섞일 수 있었다.
* `TitleSceneController`의 Host/Client 경로는 여전히 실제 서비스 호출 없이 local state만 바꾸는 prototype이므로, bootstrap 검증 완료와 real session 완료는 따로 추적해야 한다.


### 무엇을 수정했는가
* `Packages/manifest.json`에 `com.unity.services.multiplayer@2.1.3`, `com.unity.netcode.gameobjects@1.15.1`를 추가했다.
* Unity batchmode resolve로 `Packages/packages-lock.json`을 갱신하고 transitive dependency 세트를 고정했다.
* `MultiplayerServicesBootstrap`을 추가해 `UnityServices` init + anonymous sign-in + `PlayerId` 확보를 one-time init state machine으로 묶었다.
* duplicated multiplayer title scene에서만 붙는 runtime installer/driver를 추가해 `Create Room` / `Join` 앞 bootstrap, 실패 popup, `Solo Play` / `Start`의 duplicated gameplay path 우회를 연결했다.
* manual smoke check로 `Connecting...` UI와 real `PlayerId` log를 확인해 Session 2 runtime 검증을 마무리했다.
* `ProjectSettings/EditorBuildSettings.asset`에 duplicated multiplayer scene을 추가하고, `Progress_Log`, `System_Blueprint`, `Technical_Glossary`, `Multiplayer_Initialization_Session_Spec`를 현재 코드 상태 기준으로 갱신했다.


### 왜 그렇게 판단했는가
* `Session 2`의 핵심은 host/client session 로직이 아니라, 그 앞단에서 UGS/auth ready gate를 한번만 정확하게 만드는 것이다.
* main scene 안정성을 지키려면 bootstrap 진입점도 duplicated multiplayer scene path 안에서만 살아야 하므로, runtime installer + scene-path guard가 가장 안전했다.
* 아직 real Lobby/Relay/NGO host/client start는 붙지 않았기 때문에, Session 2를 닫고 바로 Session 3 Host create로 넘어가는 편이 지금 단계의 경계를 가장 분명하게 유지한다.

# attack1 준비동작 구현
## 오늘 반영한 작업
* `BossController`에 `BasicAttackSettings`를 추가해 Attack1의 `readyDuration`(초)와 `readyNormalizedWindow`(start/end)를 인스펙터에서 직접 조절할 수 있게 했다.
* `BasicAttackPattern`을 timer-only 종료 구조에서 `Animator.normalizedTime` 기반 구조로 바꾸고, 준비 구간이 끝날 때까지 `HeadDamageCaster`가 열리지 않도록 수정했다.
* `BossVisual`에 Attack1 전용 playback helper를 추가해 선택한 normalized slice가 `readyDuration`만큼 유지되도록 `Animator.speed`를 임시 제어하고, 상태 종료 시 항상 기본값으로 복구하게 했다.
* `dotnet build Assembly-CSharp.csproj -v:minimal`로 컴파일 검사를 수행해 Warning 3 / Error 0 결과를 확인했다.

## 체크리스트 업데이트
* [x] `BossController`에 `BasicAttackSettings` 추가 (`readyDuration`, `readyNormalizedWindow`)
* [x] `BasicAttackPattern`을 timer-only 흐름에서 normalizedTime 기반 흐름으로 전환
* [x] 준비 구간 동안 `HeadDamageCaster`를 비활성화하고 준비 종료 후에만 판정 오픈
* [x] 강제 종료/인터럽트 시 hitbox와 animator playback 상태를 안전하게 복구
* [x] 구현 후 `System_Blueprint`, `Technical_Glossary`, `Milestone_Backlog` 동기화

## 맥락노트
* 현재 구현은 `readyDuration`을 별도 front delay가 아니라, 선택한 normalized slice의 재생 시간을 다시 맞추는 값으로 해석한다.
* 따라서 Attack1 전체 애니메이션을 멈추는 방식이 아니라, 지정한 bite 구간만 느리게/빠르게 재생되고 준비 구간이 끝난 뒤에만 실제 bite 판정이 열린다.
* Attack1 준비동작은 Attack2가 이미 사용 중인 MinMax slider UX를 그대로 재사용해 인스펙터 일관성을 맞췄다.

## 구현 계획
### 목표
* Attack1(물기)에 준비동작 구간을 추가하고, 디자이너가 인스펙터에서 준비 시간과 준비동작 normalized 구간을 직접 조절할 수 있게 만든다.
* 준비동작 동안에는 `HeadDamageCaster`가 비활성화된 상태를 유지하고, 준비가 끝난 뒤에만 실제 bite damage window가 열리게 만든다.

### 영향 파일
* `Assets/Scripts/Boss/BossController.cs`
* `Assets/Scripts/Boss/Attacks/BasicAttackPattern.cs`
* `Assets/Scripts/Boss/BossVisual.cs` (Attack1 playback helper가 필요할 경우)
* `Assets/Editor/EditorAssemblyAnchor.cs` (기존 MinMax drawer 재사용 여부 확인)

### 변경 순서
* `BossController`에 `BasicAttackSettings` 직렬화 데이터를 추가하고, `readyDuration`과 `readyNormalizedWindow`를 인스펙터에서 조절 가능하게 만들었다.
* `OnValidate()`에서 Attack1 준비 구간 값을 `0~1` 범위로 clamp하고, 종료값이 시작값보다 작아지지 않도록 보정했다.
* `BasicAttackPattern`을 단순 `attackDuration` 타이머 종료 구조에서, Animator `normalizedTime`을 읽는 구조로 바꿨다.
* 준비 구간 동안에는 hitbox를 열지 않고, 선택한 normalized window가 끝난 뒤에만 `HeadDamageCaster.EnableHitbox(...)`를 호출하도록 수정했다.
* `BossVisual`에 attack 전용 playback speed 제어 경로를 추가하고, 종료/인터럽트 시 기본값으로 복구하도록 구현했다.

### 설계 근거
* `System_Blueprint`는 패턴별 공격 타이밍을 인스펙터 값으로 조절 가능한 구조를 이미 사용 중이며, Attack2가 같은 방식의 `damageCastNormalizedWindow`를 사용하고 있다.
* Attack1 준비동작도 Controller가 설정 데이터를 들고, Pattern이 실행 타이밍을 해석하는 구조가 현재 Boss 공격 아키텍처와 가장 잘 맞는다.
* DamageCaster는 실제 판정 실행기이므로, 준비동작 동안 끄고 준비 종료 후에만 여는 방식이 요구사항과 가장 직접적으로 맞는다.

### 리스크
* Attack1 playback speed를 조절할 경우, 종료 시 speed 복구가 누락되면 이후 Locomotion이나 다른 공격 애니메이션에 영향이 갈 수 있다.
* CrossFade 구간이나 상태명 불일치가 있으면 `normalizedTime` 기반 준비 구간 판정이 늦게 시작되거나 누락될 수 있다.
* `readyDuration = 0` 또는 매우 짧은 값에서 현재 bite와 거의 같은 체감이 나와야 하므로 edge case 처리가 필요하다.
* 피격/사망/강제 전환 시 Attack1 hitbox가 남아 있지 않도록 exit 정리가 확실해야 한다.

### 검증 포인트
* `readyDuration = 0`일 때 현재 Attack1과 거의 같은 체감으로 동작해야 한다.
* `readyNormalizedWindow` 슬라이더를 옮기면 준비동작으로 취급되는 bite 구간이 함께 바뀌어야 한다.
* 준비동작 동안 `HeadDamageCaster`가 열리지 않고, 준비 종료 후에만 damage가 들어가야 한다.
* Attack1 도중 피격/사망/상태 전환이 발생해도 hitbox와 playback 상태가 정상 복구되어야 한다.
* 코드 구현 후 `dotnet build Assembly-CSharp.csproj -v:minimal`로 컴파일 검증을 수행해야 한다.

### 문서 동기화 계획
* 기준 로그는 `docs/Progress_Log/2026-03-16.md`를 사용한다.
* 코드 구현 완료 후 `docs/technical/System_Blueprint.md`에 Attack1 ready tuning rule과 inspector 노출 상태를 반영했다.
* 코드 구현 완료 후 `docs/technical/Technical_Glossary.md`에 Attack1 ready 관련 용어를 추가했다.
* 구현 완료 후 `docs/roadmap/Milestone_Backlog.md`의 Attack1 준비동작 항목 상태를 함께 갱신했다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v:minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* 현재 live `BasicAttackPattern`은 `Enter()`에서 bite 애니메이션 재생과 동시에 `HeadDamageCaster.EnableHitbox(...)`를 호출하므로 Attack1에는 준비동작이 없다.
* Attack2는 이미 `MinMaxRangeAttribute` 기반 `Vector2 damageCastNormalizedWindow`를 사용하고 있어, Attack1도 같은 인스펙터 UX를 재사용할 수 있다.
* `BossVisual`은 기존에 locomotion speed 파라미터 제어만 있었고, Attack1 준비 구간을 늘이기 위한 `Animator.speed` 복구 helper는 없었다.
* 첫 빌드에서 `BossController.BasicAttackSettings` nested type과 같은 이름의 property가 충돌해 컴파일 에러가 발생했고, accessor 이름을 `BasicAttackConfig`로 바꾸며 해결했다.

### 무엇을 수정했는가
* `BossController`에 `BasicAttackSettings`와 clamp 로직, `BasicAttackConfig` accessor를 추가했다.
* `BasicAttackPattern`을 normalizedTime 기반으로 바꾸고, 준비 구간 동안 hitbox를 닫은 채 유지하다가 준비 종료 후 damage window를 여는 흐름으로 수정했다.
* `BossVisual`에 `SetAnimatorPlaybackSpeed`, `ResetAnimatorPlaybackSpeed`, `GetBasicAttackClipLengthOrDefault` helper를 추가해 Attack1 ready slice 재생 시간을 직접 제어하게 했다.
* `Progress_Log`, `System_Blueprint`, `Technical_Glossary`, `Milestone_Backlog`를 구현 결과 기준으로 동기화했다.

### 왜 그렇게 판단했는가
* 사용자 요구사항의 핵심은 "how long it readies"와 "which part of the bite animation is ready motion"을 인스펙터에서 직접 조절하는 것이므로, 단순 고정 딜레이보다 normalized window 기반 설계가 더 잘 맞는다.
* 현재 Boss 공격 구조는 `Controller = config holder`, `Pattern = timing executor` 역할 분리가 분명하므로, Attack1도 같은 구조를 유지하는 편이 유지보수와 문서 일관성에 유리하다.
* 준비동작 동안 damage caster를 끄고, 종료/인터럽트 시 `Animator.speed`와 hitbox를 모두 정리해야 회귀 리스크를 가장 작게 만들 수 있다.
